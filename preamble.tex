\usepackage[
  margin=3cm,
  left=3cm,
  right=2cm,
  top=2cm,
  bottom=3cm
]{geometry}

\usepackage{newtxtext}
\usepackage{newtxmath}
\usepackage{setspace}
\onehalfspacing
%\sloppy

\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{siunitx}
\usepackage{hyperref}
\hypersetup{
  hidelinks,
  pdftitle={Effects of Biochar as a Peat Alternative on Salt Stress Responses of Four Mint Species (Mentha piperita, Mentha spicata, Mentha crispata and Mentha suaveolens)},
  pdfauthor={Nuengmanixai SILIPHON},
}
\usepackage[nopatch=footnote]{microtype} % newtx's Times config has no footnote-patch target; this silences the harmless warning
\usepackage{bookmark}
\usepackage{xcolor}
\usepackage{parskip}

% --- Number figures/tables per chapter (Figure 3.1, Table 4.2, ...) ---
\usepackage{chngcntr}
\counterwithin{figure}{chapter}
\counterwithin{table}{chapter}

\usepackage[
  backend=biber,
  citestyle=authoryear,
  bibstyle=numeric,
  sorting=nyt,
  language=english
]{biblatex}
\setcounter{biburlnumpenalty}{9000}
\setcounter{biburlucpenalty}{9000}
\setcounter{biburllcpenalty}{9000}

\usepackage{csquotes}

\addbibresource{references.bib}

% --- Page numbers: bottom-right, continuous (per university instructions) ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[R]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[R]{\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% --- Captions: tables above, figures below (per university instructions) ---
\usepackage{caption}
\captionsetup[table]{position=top}
\captionsetup[figure]{position=bottom}

% --- Make author names uppercase in bibliography ---
\DeclareNameFormat{author}{%
  \nameparts{#1}%
  \MakeUppercase{\namepartfamily}%
  \ifblank{\namepartgiven}{}{\addcomma\space\MakeUppercase{\namepartgiven}}%
  \ifblank{\namepartprefix}{}{\space\MakeUppercase{\namepartprefix}}%
  \ifblank{\namepartsuffix}{}{\space\MakeUppercase{\namepartsuffix}}%
  \usebibmacro{name:andothers}%
}